import { ToggleHandlingDirective } from './toggle-handling.directive';

describe('ToggleHandlingDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleHandlingDirective();
    expect(directive).toBeTruthy();
  });
});
